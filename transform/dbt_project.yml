name: jp_quant
version: "0.1.0"
config-version: 2
profile: jp_quant

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  jp_quant:
    staging:
      +materialized: view

seeds:
  jp_quant:
    +schema: raw
